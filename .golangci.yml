version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - revive
    - gosec
    - ineffassign
    - unused
  settings:
    govet:
      enable:
        - shadow
    revive:
      rules:
        - name: exported
          arguments: [checkPrivateReceivers]
        - name: context-as-argument
        - name: error-strings
        - name: unused-parameter
  exclusions:
    generated: lax
    paths:
      - gen/
    rules:
      - path: _test\.go
        linters:
          - gosec
      # The simulator clamps every narrowing conversion explicitly (min/max);
      # G115 would need a nolint on nearly every line of the node emulation.
      - path: internal/sim/
        linters:
          - gosec
        text: G115

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/smhunt/sumpnet
  exclusions:
    generated: lax
    paths:
      - gen/
